const superHeros: string[] = [] //syntax for string array
const heroPower: Array <number> = []


type User = {
    name: string
    isActive: boolean
}

const allUser: User[] = []

allUser.push({name: '' , isActive: true})

superHeros.push('spiderman')

heroPower.push(4)




export {}
